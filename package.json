{
  "name": "helios-scada-root",
  "private": true,
  "version": "2.4.0",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "dev": "cd frontend && npm run dev"
  }
}
